<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.dao.ClassDao">
    <insert id="insertClass">
        insert into class (name,p_id)
        values (#{name},#{pId})
    </insert>
    <update id="updateClassById">
        update class
        set name = #{name},p_id=#{pId}
        where id = #{id}
    </update>
    <delete id="deleteClassById">
        delete
        from class
        where id = #{id}
    </delete>
    <select id="getClassPageList" resultType="com.example.model.Class">
        select class.id, class.name,p_id professionId, p.name professionName
        from class
        left join profession p on class.p_id = p.id
    </select>
    <select id="getClassById" resultType="com.example.model.Class">
        select class.id, class.name,p_id professionId, p.name professionName
        from class
                 left join profession p on class.p_id = p.id
        where class.id = #{id}
    </select>
</mapper>