<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.dao.UserDao">

    <update id="updateUserInfoByUsername">
        update t_users
        <set>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatarUrl != null">
                avatar_url = #{avatarUrl},
            </if>

            <if test="homeUrl != null">
                home_url = #{homeUrl},
            </if>

            <if test="nickName != null">
                nick_name = #{nickName},
            </if>

            <if test="password != null and password !=''">
                `password` = #{password},
            </if>
        </set>

        where username = #{username}
    </update>
    <select id="getUserInfoByUserName" resultType="com.example.model.Users">
        select ui.id, username, password, name, phone, role, role_name, `describe` as role_describe
        from user_info as ui
        left join role r on r.id=ui.role
        where username = #{username}
    </select>
    <select id="findEmailCount" resultType="java.lang.Integer" parameterType="java.lang.String">
        select count(1) from t_users where email = #{email}
    </select>

    <insert id="insertUser">
        insert into t_users (username, password, email, avatar_url, home_url, nick_name,group_name)
        values (#{username}, #{password}, #{email}, #{avatarUrl}, #{homeUrl}, #{nickName},#{groupName})
    </insert>
</mapper>